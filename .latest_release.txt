# 0.6.1
Merge pull request #24 from jcarrag/fix_magnet_hash_extraction

Fix: stripping the prefix from the magnet hash when not present
Fix: magnet_url::Magnet already strips the prefix from the magnet hash

Update impl so that it can handle both the prefix existing and not
